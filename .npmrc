# Use hoisted node_modules layout (flat like npm) instead of symlinks
# This is needed for Docker builds where pnpm symlinks don't work
node-linker=hoisted

# Ensure workspace packages are properly accessible
shamefully-hoist=true
